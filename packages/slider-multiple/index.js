import SliderMultiple from './src/main';

/* istanbul ignore next */
SliderMultiple.install = function (Vue) {
    Vue.component(SliderMultiple.name, SliderMultiple);
};

export default SliderMultiple;
